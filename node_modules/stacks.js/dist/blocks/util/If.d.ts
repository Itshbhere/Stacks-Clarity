import Block from "../block.js";
/**
 * @ignore
 */
export default class IfBlock extends Block {
    condition: boolean;
    static If(condition: boolean): IfBlock;
    constructor(condition: boolean);
    then(block: Block): this;
    else(block: Block): this;
}
